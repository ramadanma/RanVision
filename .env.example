MYSQL_ROOT_PASSWORD=changeme_root
MYSQL_DATABASE=ranvision
MYSQL_USER=ranvision
MYSQL_PASSWORD=changeme_db
REDIS_PASSWORD=changeme_redis
# openssl rand -hex 32
SECRET_KEY=changeme_secret_key_at_least_32_chars_long_replace_this
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=changeme_fernet_key_replace_this

# 仅在 venv 直接运行时需要（Docker 模式由 docker-compose.yml 注入，无需在此填写）
# Docker 模式：MySQL 端口 3307，Redis 端口 6381（见 docker-compose.example.yml）
# DATABASE_URL=mysql+aiomysql://ranvision:changeme_db@127.0.0.1:3307/ranvision
# REDIS_URL=redis://:changeme_redis@127.0.0.1:6381/0

# Phase 2: ML 推理
YOLO_MODEL_PATH=yolov8n-pose.pt
GPU_COUNT=4
FACE_SIM_THRESHOLD=0.5
